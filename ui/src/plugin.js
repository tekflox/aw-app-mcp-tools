export function register() {
  // No core.nav contribution — this app's tool count has no business
  // showing up in the global top nav; settings (the gear) is where a user
  // looks for it.
}

export default register;
