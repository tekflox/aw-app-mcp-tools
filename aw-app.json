{
  "manifest_version": 1,
  "id": "mcp-tools",
  "name": "MCP Tools",
  "version": "0.5.2",
  "description": "Installs MCP helper tools for Agentic Workspace, starting with a ready-to-use Playwright MCP config for shared browser automation.",
  "tier": "inprocess",
  "requires_ui_refresh": true,
  "category": "Tools",
  "publisher": "TekFlox",
  "resource_estimate": {
    "cpu": "low",
    "memory": "low",
    "disk": "low"
  },
  "runtime": {
    "python": ">=3.11",
    "entrypoint": "mcp_tools_app.plugin:McpToolsAppPlugin",
    "pip_requires": [],
    "standalone": {
      "module": "mcp_tools_app",
      "default_port": 9406
    }
  },
  "permissions": [
    "commands:install",
    "fs:workspace-data",
    "routes:register",
    "ui:code"
  ],
  "contributes": {
    "system_clis": [
      {
        "name": "aw-playwright-mcp",
        "installer": "scripts/install_playwright_mcp.sh"
      }
    ],
    "routes": [
      {
        "prefix": "/api/apps/mcp-tools"
      }
    ],
    "frontend": {
      "mode": "component",
      "bundle": "ui/dist/mcp-tools.js"
    },
    "mcp": {
      "reload_on_save": true
    }
  },
  "config_schema": {
    "type": "object",
    "properties": {
      "mcpServers": {
        "type": "object",
        "title": "MCP servers",
        "description": "Written verbatim to this app's mcp.json \u2014 add, remove, or edit any MCP tool definition here (same shape as a .mcp.json's mcpServers). Ships with Playwright pre-configured for the shared AW browser.",
        "default": {
          "playwright": {
            "enabled": true,
            "type": "stdio",
            "command": "npx",
            "args": ["-y", "@playwright/mcp@0.0.77", "--cdp-endpoint", "http://aw-app-browser:9223"]
          }
        }
      }
    },
    "required": []
  },
  "dependencies": {
    "apps": [
      {
        "id": "mcp-gateway",
        "version": ">=0.1.0",
        "required": true,
        "reason": "MCP Tools contributes mcp.json definitions that are discovered and merged by the MCP Gateway app."
      }
    ],
    "note": "Runtime-enforced app dependency: aw-workspace installs/loads mcp-gateway before mcp-tools so the gateway can scan mcp-tools/mcp.json."
  },
  "migrations": {}
}
